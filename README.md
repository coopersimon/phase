# Phase : Playstation Emulator

This project is split into 2 parts: phase, the emulator core. This just deals with emulation and does not handle I/O. phase-bin is a thin wrapper that handles the window and user interface.